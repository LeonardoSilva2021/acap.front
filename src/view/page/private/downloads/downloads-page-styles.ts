import { createUseStyles } from "react-jss";

export const useDownloadsPageStyles = createUseStyles({
    root: {
        overflow: 'auto',
        height: '100%'
    },
    conteudo: {
        display: 'flex',
        height: '100%'
    }
})