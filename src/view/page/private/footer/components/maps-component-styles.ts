import { createUseStyles } from "react-jss";

export const useMapsComponentStyles = createUseStyles({
    root: {
        height: "100%", 
        width: '100%', 
        borderRadius: '16px',
    }
})