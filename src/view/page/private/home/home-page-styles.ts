import { createUseStyles } from "react-jss";

export const useHomePageStyles = createUseStyles({
    root: {
        overflow: 'auto',
        height: '100%'
    },
    conteudo: {
        display: 'flex',
    },
})